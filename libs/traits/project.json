{
  "name": "traits",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/traits/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/traits"
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["coverage/libs/traits"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "../../coverage/libs/traits"
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["libs/traits/**/*.ts"]
      }
    }
  },
  "tags": []
}
